const users = [];
function register(event) {
  const lst = [1, 2, 3, 4, 5];
  event.preventDefault();
  console.log();
  const formData = new formData(event.target);
  lst.forEach((number) => {});
  console.log(Number);
}
